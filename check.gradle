task check (type : Exec) {
    print 'Check task on private-compute-cloud:'
    workingDir '.'
    // Run the default tox environment.
    executable = "tox"
    args "-r"
}

task do_all{
  check.execute();
}
